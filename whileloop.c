#include<stdio.h>
    int main()
    {
        int i = 1;//Initialize variable i with value 1 

        while(i <= 5)//Loop will run until i become greater than
        {
                printf("%d\n", i); //print the value of i
                i++; // Increment i by 1
        }
        return 0; // End of program
    }