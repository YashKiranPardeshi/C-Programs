#include<stdio.h>

    int main()
    {
        int age = 20;   //Integer variable age stores value 20

        if(age >= 18) //Check if age is 18 or greater
        {
            printf("You are eligible to vote"); //Print message
        }

         return 0; // End of Program
    }